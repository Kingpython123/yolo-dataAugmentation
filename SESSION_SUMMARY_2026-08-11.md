# 会话总结：缺陷库二次扩容 + 定向补充生成 + 演示素材整理

记录时间：2026-08-11。用于在新会话里快速恢复上下文，不是给最终用户看的说明文档。

本次会话是在上一次会话（桌面程序封装 + 定向生成功能，commit `d18af6b`/`6a7d43c`）
基础上继续的，代码结构和约束沿用上一份 SESSION_SUMMARY 记录的内容（已被本文件覆盖，
如需查历史可以 `git log -p .kiro/SESSION_SUMMARY.md` 或 `git show <老commit>:SESSION_SUMMARY.md`）。

---

## 一、这次会话做了什么

### 1. 缺陷库二次扩容：668 → 838 条

用户在"实拍正样本(有缺陷)"数据集里补充了更全的图片（用户原话"我原本的有缺陷样本不全，
把这部分更全的图片都补充进去了"）。跑 `run.py build-catalog`（增量模式，自动跳过已分析过
的 479 张），新分析 133 张，新增 170 条，668 → 838。

关键坑：用户给了6个"人工挑出来的漏检形态相似"文件名，反查发现5个在缺陷库里找不到——
根因是这5张图当时还没补充进数据集，用户确认后重新扫描才找到。**这5张图 + 之前旧库里已有
的1张`3.jpg`共6条，构成后续"定向补充"任务2的参考池**（第6张`4_20260426161348952.jpg`
经VLM判定确实无明显缺陷，用户确认不用管，最终只用其余6条entry_id，其中一张图出了2条）。

### 2. 两个定向补充生成任务（用户明确要求不做severity过滤）

用户反馈"实验发现缺陷较轻微的漏检率更高"，要求这两批**不使用** `usable_references()`
的severity过滤，全部170条/6条参考(含severity1-2)都要用上：

- **任务1**：170条新增缺陷 → 3.jpg，精确凑240张（70条参考各出2张 + 100条各出1张）
- **任务2**：6条指定参考（上面反查出的那6条） → 4.jpg，60张（6条×10）

实现方式：不是走 `run.py gen-augment` 命令行（PowerShell传中文entry_id会编码损坏，
实测报错"找不到条目"是乱码），而是直接写脚本调用 `plan_augment_by_references()` /
`augment_by_references()` 内部函数。脚本留在项目根目录：
- `_tmp_run_two_tasks.py` — 主执行脚本，按顺序跑 任务1-A → 任务2 → 任务1-B
  （用户中途要求把顺序从"1-A→1-B→2"改成"1-A→2→1-B"，先跑完1-A才能安全改文件重启,
  改磁盘文件不会影响已在运行的进程内存)
- `_tmp_plan_final.py` — 生成 `_tmp_group_a.txt`/`_tmp_group_b.txt`（170条的两个分组）
- `_tmp_six_entries.txt` — 任务2的6条entry_id

**中途踩坑记录（对新会话有用）**：
1. 跑到一半API欠费(`insufficient_user_quota`)，403错误。当前 `_classify_failure()`
   把这类归到笼统的`other`，**没有专门识别余额不足**，是个已知缺口，之后可以加
   `insufficient_balance` 分类。
2. 用户充值后**立刻续跑还是失败**（报错里的负余额数字和欠费时完全一样），说明是
   充值到账有延迟，不是脚本问题。**教训：续跑前务必先 `run.py test-api` 确认两项都OK
   再执行正式生成**，否则白跑一轮（虽不花钱但浪费时间）。
3. 用户怀疑VSCode里手动跑的命令退出软件后可能变孤儿进程继续跑، 用
   `Get-CimInstance Win32_Process -Filter "Name='python.exe'"` 查真实系统进程
   （不能只看 `list_processes` 工具自己的管理列表，那个只覆盖本工具启动的进程）,
   确认没有残留。
4. 全程反复出现同一个**未解决的bug**：
   `mask dimensions 768x2048 do not match source dimensions 576x1536`
   （以及 768x1969/599x1536、768x1765/668x1536 等多种尺寸组合），HTTP 400,
   `code:invalid_image`。三次重试全部同样报错，**确定性失败不是随机网络问题**。
   本次会话没有定位根因，只是识别出来了。怀疑是 `adaptive_patch_size()` /
   `openai_edit_mask()` 计算出的mask尺寸与实际裁块尺寸不一致的边界情况，
   **下次会话如果继续处理生成任务，遇到这个报错的样本值得专门排查**。

最终两个任务都跑完，产出通过 `_run_tasks` 的常规质检把关（不因为"定向补充"放松标准）。

### 3. 四类瓶子演示素材整理（用户多轮追加需求，纯本地操作+3轮小规模生成）

用户要"1.jpg/2.bmp/3.bmp/5.1 各一张真实无缺陷图 + 3张对应生成的褶皱图，放新文件夹"。

输出目录：`D:\zlf\project\photo_difussion\演示素材_四类瓶子褶皱对比\`
（**在项目外，不受.gitignore管理，不会进git**）

过程：
1. 先用annotations.jsonl里已有产出凑，1.jpg充足(112张干净图各有合格产出)，但
   2.bmp/3.bmp合格记录只有1~2条，用户明确说"合格+驳回都行，不够就直接生成"
2. 初版每类3张，全用demo已用过的干净图+对应产出，直接复制（脚本已删，逻辑见下）
3. 用户陆续追加3轮生成需求，**核心模式都是：固定同一张干净图不变，只换缺陷库参考**：
   - 追加1："2.bmp/3.bmp/5.1 从缺陷库选同类别样本再生成"→ 高severity低count优先
   - 追加2："再生成轻微的缺陷" → 严格只挑`defect_type=='变形'`(排除划痕，保持主题一致)
     的最低severity候选，1.jpg/2.bmp/3.bmp各生成3/3/2张
   - 追加3(**关键发现，后被追加4修正/细化**)："为什么2.bmp/3.bmp外形变严重了" →
     排查发现是`seam_continuity`(接缝连续性)崩了(=2分，门槛5)，VLM原话反复出现
     "贴片状重绘""标签断层""瓶身轮廓被切割外扩到背景"。`bg_change`硬约束显示掩膜外
     变化=0.0，说明约束本身没问题，是掩膜**内部**编辑模型重绘过量。改选bbox更紧凑
     (宽高45~191像素)、不落标签区、severity=3的候选重新生成2+2+2张，**结果1.jpg
     两张全合格，但2.bmp/3.bmp依然全部因seam_continuity=2驳回**。当时结论是"`.bmp`
     格式这两类瓶子本身编辑模型处理裁块边缘不稳定"——**这个结论后来被追加4的数据
     证明不完整，见下**。

   - 追加4(**同一会话继续追问后的关键修正**)：用户要求"2.bmp再生成2张，挑逼真的"。
     没有直接挑severity/count，而是先查了`outputs/annotations.jsonl`里这条demo干净
     图(`2_20250729121916968.bmp`)历史上跑过的全部15条记录，发现一个决定性规律：

     ```
     2.bmp本类参考在这张干净图上: 9次尝试, 0次合格(0%合格率), 最高分5.75
     跨类参考(3.bmp/5.3)在这张干净图上: 2次尝试, 2次都是当前最高分(7.25/6.90, 均合格)
     ```

     按"历史平均分最高、合格率100%"的逻辑先选了2条**2.bmp本类**参考去生成，结果
     两条都驳回(4.25/5.7分)，跟"历史合格率100%"的预期完全相反——因为那个100%合格率
     统计的是这两条参考在**其他干净图**上的表现，换到这张图上不适用。改用跨类策略
     (从全局历史记录里找realism最高、变形类型、未在此图用过的参考，来源类别是
     4.jpg/3.jpg)重新生成，结果明显改善: 7.0分合格 + 6.05分驳回(且驳回只差
     seam_continuity一项没过门槛，其余5维度全在7-9分)。

     **修正后的结论**：不是"`.bmp`格式瓶子本身不稳定"，而是**"同类参考复刻到同类
     目标上"这种组合本身不稳定，跨类反而更稳**。推测机制(**未做对照实验验证，
     样本量也小，只是数据方向一致的合理推测**)：参考裁剪图(image 2)和目标裁块
     (image 1)如果来自同一类瓶子，两者标签设计几乎相同但角度/位置/裁剪范围不完全
     对齐，模型可能会倾向于"对照/拼接"两份相似但不对齐的标签内容，产生错位、断层、
     悬空碎片；换成跨类参考，标签设计明显不同，模型更容易只提取几何、忽略参考的
     印刷内容，拼接痕迹更少。**这个假说值得下次会话如果继续处理设计验证:
     对比"同类参考"和"跨类参考"在更大样本量上的seam_continuity均值差异是否统计显著，
     以及检查`structure_ref.py`的浮雕图转换是否真的把参考的印刷内容滤干净了
     (如果没滤干净，可能是本类参考"漏了"印刷信息进prompt的直接原因)**。

     这也意味着**之前很多批次"从缺陷库选同类别样本"的默认做法(追加1/追加2/以及
     `gen-target`/`gen-sweep`等主命令的默认跨类比例`cross_class_ratio: 0.35`)可能
     没有用上这个"同类反而不稳"的规律** —— 当前35%跨类比例是否足够、要不要针对
     `.bmp`类特别调高跨类比例，值得后续验证。

每轮生成后都用同一套模式追加进文件夹：文件名 `{序号}_褶皱图_{合格/驳回}_realism{分}_{标签}.png`，
序号接续现有最大编号，不覆盖已有文件。脚本模式是: 查annotations.jsonl里
`"__demo"`/`"__light"`/`"__medium"`/`"__realistic"`/`"__final"` 等命名标记的产出记录
→ 按realism排序 → shutil复制。这些中间脚本本次会话结束时**没有清理**，仍留在
`defect_synthesis/`目录下(`_tmp_*.py`)，新会话可以直接看或删除。

**当前文件夹真实状态(2026-08-11会话结束时核对过，与会话前的记录不完全一致，
`2.bmp`实际文件数比之前总结少，原因未查，可能是文件被移动或之前统计有误，
如果需要历史那批文件建议先确认是否被误删)**：
```
1.jpg  9张(0原图+8褶皱)
2.bmp  5张(0原图+4褶皱: 1_2_3合格, 4驳回)  ← 本次追加4之后的真实状态
3.bmp  11张(0原图+10褶皱)
5.1    7张(0原图+6褶皱)
```
**下次会话打开文件夹前建议先`Get-ChildItem`核对一遍真实文件数，不要直接信任
本文档里记录的数字。**

---

## 二、代码改动（已提交到git的部分）

上次会话结束时commit到`6a7d43c`。**本次会话没有对`src/`下的核心代码做改动**
（只是使用现有的`plan_augment_by_references`/`augment_by_references`/
`usable_references`等函数），所有新脚本都是`_tmp_*.py`临时脚本，**没有提交到git**。

## 三、遗留的临时文件（下次会话可以清理或复用）

项目根目录下还有这些`_tmp_*`文件（部分是这次会话中间产物，部分之前会话遗留）：
- `_tmp_list_refs.py` / `_tmp_refs_list.txt` — 早前"9张漏检截图形态检索"用的，
  跟本次任务1/2无关，可删
- `_tmp_run_augment.py` — 早前"500张"方案的旧脚本，已废弃，可删
- 其余`_tmp_*.py`本次会话结束前已用`del`清理，但如果中途有工具调用失败，
  可能有少量遗漏，新会话可以`Get-ChildItem _tmp_*`检查一下

`D:\zlf\project\photo_difussion\_tmp_six_entries.txt` 等分组文件在
`defect_synthesis/`目录下，如果任务1/2需要复现或核对，这些文件还在。

## 四、关键数据现状（新会话如果要继续生成任务，先核对这些数字）

- 缺陷库：838条（`outputs/catalog/catalog.json`），`analyzed.json`记录已分析
  的612张有缺陷源图(479+133)
- `min_reference_severity_by_type`: 划痕≥2，其余(变形)≥3，这是**默认**门槛,
  本次两个定向补充任务(170条/6条)是**显式绕过**这个门槛(不调用`usable_references`)
  的特例做法，不是改了默认配置
- `outputs/annotations.jsonl` 里已经积累了大量记录，文件名里的标记后缀
  (`__t`/`__aug`/`__demo`/`__light`/`__medium`等)代表不同批次来源，
  排查问题时按后缀过滤记录即可定位是哪次会话生成的

## 五、如果新会话要继续处理的优先级建议

1. **验证"同类参考复刻到同类目标不稳定, 跨类反而更稳"这个假说**(本次会话最新
   发现，见上文"追加4")——如果在更大样本上验证成立，应该考虑调整
   `cross_class_ratio`(当前默认0.35)，尤其是`.bmp`类目标可能需要更高跨类比例。
   同时值得检查`structure_ref.py`的浮雕图是否真的把参考的印刷内容/标签设计滤干净，
   这可能是本类参考"泄露"标签信息进prompt导致模型对照拼接的直接原因
2. `mask dimensions xxx do not match source dimensions yyy` 的确定性400错误,
   多次在不同尺寸组合上复现，值得定位是`adaptive_patch_size`还是
   `openai_edit_mask`的计算逻辑有边界条件bug
3. `_classify_failure()`补充`insufficient_balance`分类，避免余额问题被
   笼统归入`other`导致排查时误判成随机网络问题
4. 清理项目根目录下的`_tmp_*`遗留文件(这次会话结束时没清理，数量比上次更多了)
5. 核实`演示素材_四类瓶子褶皱对比\2.bmp\`文件数异常减少的原因(会话内发现但未深究)
