# 会话总结：架构路线决策（转向 demo 架构）+ 样本类别按产品重命名

记录时间：2026-08-26。用于新会话快速恢复上下文，不是给最终用户看的说明文档。
上一次的会话记录见 `SESSION_SUMMARY_2026-08-11.md`（那份此前一直未提交，本次一并入库）。

> **本次提交的范围说明**：工作区里混着三个会话的未提交改动，本次一并提交，归属如下。
> - **2026-08-11 会话**：缺陷库 668→838 扩容（`outputs/catalog/*.json` + 170 张新裁剪图）、
>   `SESSION_SUMMARY_2026-08-11.md`
> - **flat-edit / relief-transfer 探索期（无会话记录）**：`src/mask_utils.py`(+404)、
>   `src/generate.py` 的 flat-edit/relief 部分(+276)、`src/quality_check.py`(+23)、
>   `config.yaml` 的 relief 参数段、`tools/regression_check.py`(+61)、
>   `tools/check_orientation_match.py`。**这两条路线本次已确认放弃**（见第四节），
>   代码保留作为历史记录，不再维护。
> - **本次（2026-08-26）**：下面第一~三节的全部内容。

---

## 一、发现并修复了一个数据完整性 bug：类别名的产品混淆

### 问题

用户人工核对出：类别目录 `1~4.bmp` 在两个数据集根目录下指的是**不同产品**。

| 旧类别名 | 无缺陷目录里其实是 | 有缺陷目录里其实是 |
|---|---|---|
| `1~4.jpg` | 500多效极润 | 500多效极润 |
| `1~4.bmp` | **60ml多效极润**（675×1370） | **120蓝瓶**（796×1656） |
| `5.1~5.4` | 500水次方 | 500水次方 |
| `120蓝瓶` | 120蓝瓶（只有角度4） | （无此目录） |
| `360森呼吸` | 360森呼吸 | 360森呼吸 |

而流水线按**同名类别目录**配对 `clean_root/<类别>` 与 `defect_root/<类别>`，且

```python
"cross_class": ref.get("class_name") != class_name    # 只是目录名的字符串比较
```

于是两个方向都错：拿 60ml白瓶当目标 + 120蓝瓶的缺陷当参考被记成**同类**；
拿 120蓝瓶当目标 + `1.bmp` 的参考（也是120蓝瓶）被记成**跨类**。

**实测影响**：缺陷库 212/838 条（25.3%）产品归属标错；主产线 1257 条标注里 342 条涉及。
另外 `120蓝瓶` 因为 `defect_root` 下没有同名目录（类别是按 defect_root 自动扫描的），
它的 90 张干净图**从来没被用过**。

顺带发现：`1~4.jpg` / `5.1~5.4` 这些"类别"其实是**同一产品的不同拍摄角度**
（`5.1~5.4` 的文件名 `500水次方一角度/二角度/...` 直接印证）。所以
`cross_class_ratio: 0.35` 的真实含义一直是"跨**角度**比例"，不是跨产品。

### 修法：类别名改成 `<产品>_角度<N>`

改后：无缺陷 17 个类别 / 有缺陷 16 个类别，覆盖 5 个产品。

```
500多效极润_角度1~4    暗背景 1200×2400
60ml多效极润_角度1~4   亮背景有桌布 675×1370  ← 只有干净图, 无缺陷样本
120蓝瓶_角度1~4        透明瓶 796×1656        ← 干净图只有角度4
500水次方_角度1~4      暗背景 1200×2400
360森呼吸_角度1~4      1030×2350             ← 缺陷图未编目
```

**没用嵌套 `产品/角度/` 结构**，因为 `src/rejection.py` 用
`clean_root / class_name / f"{clean_stem}{ext}"` 直接拼路径、
`packaging/make_catalog_pack.py` 也按 `<root>/<class>/<文件名>` 重建 `source_image`，
两者都假设类别目录只有一层，嵌套会让 `regen-rejected` 与打包脚本同时失效。
扁平命名按产品名排序后同样自然聚合，这两处零改动。

**保留"角度"作为类别粒度**（不合并成产品），因为现有 `allow_cross_class` /
`cross_class_ratio` 语义不变、仍可按角度分层，产品级判断由新增的
`dataset.product_of()` 提供。

### 迁移执行（工具已入库）

- `tools/migrate_classes_by_product.py` — 迁移主脚本，默认 dry-run，`--apply` 执行，
  `--undo <反向映射>` 撤销目录改名。带迁移前自检（目标名冲突 / 未覆盖类别 /
  文件名前缀与产品判断是否一致）
- `tools/migrate_output_filenames.py` — 产出文件名里嵌着旧类别前缀与旧 `entry_id`
  （`rejection.py` 的反查靠它），一并迁移
- `tools/verify_product_migration.py` — 迁移后一致性验证，把"迁移前既有问题"作为
  基线排除，只对**迁移引入**的破损判失败

实际改动量：

| 项 | 数量 |
|---|---|
| 样本目录重命名 | 35 个目录 / 2995 张图（目录级 rename，瞬间完成） |
| 缺陷库 | 838 条的 `class_name`/`entry_id`/`crop_path`/`source_image` + 838 个裁剪图改名 |
| `analyzed.json` | 612 条路径改写 —— **不改会导致增量编目重跑 612 次 VLM** |
| 标注 | 1257 + 20 + 20 条改写，新增 `cross_product` 字段 |
| 产出文件/目录 | 3060 项改名（generated / masks / rejected / debug） |

代码同步：`src/dataset.py` 新增 `product_of()` / `same_product()`；
`src/generate.py` 标注新增 `cross_product`（保留 `cross_class` 原语义以便与历史数据比较）；
`config.yaml`、`README.md`、`run.py`、`src/catalog_query.py`、
`app/services/dataset_service.py` 的类别名示例与注释；
项目根 `缺陷合成流程详细说明.md` 加了旧→新类别对照表（正文不动，那些名字与历史实测数据绑定）。

安全网：`outputs/product_migration_reverse_map.json`（可 `--undo`）+
`catalog.json` / `analyzed.json` / 三份 `annotations.jsonl` 的 `.pre_product_migration`
备份。这些都已加进 `.gitignore`（本机安全网，含绝对路径，不入库）。

### 迁移中自己踩的坑（新会话注意）

1. 第一轮迁移改写了 `测试8.13` / `测试8.13_batch1` 的标注路径，但**忘了重命名那两个
   目录下的实际文件夹和文件**，导致 40 条 `generated_image` 指向不存在的路径。已补齐。
2. `catalog.json` 的 `source_image` **既有绝对路径也有相对路径**
   （`packaging/make_catalog_pack.py` 会归一化成 `<defect_root>/<类别>/<文件名>`）。
   相对路径必须以 `defect_synthesis/` 为基准解析，否则换个工作目录跑就会静默判为
   "文件不存在"，把 94 条参考悄悄丢掉、让参考池有偏。
3. `experiments/` 下早几轮实验留的 JSON 里记的是旧绝对路径，已加
   `experiments/ab_arch/legacy_paths.py` 做读取时映射（不改写历史记录）。

### 验证结果

`tools/verify_product_migration.py` 全部通过。`selftest`、`inspect`、
`requalify`（离线重判）均正常。

顺带查出**两个迁移前就存在的问题**（不是本次造成的）：

- **驳回样本的 mask 从未被写出**：`outputs/rejected` 下有 0 个 `*_mask.png`，
  但 56 条标注记录了指向 `rejected/` 的 mask 路径。流水线对驳回样本只写图不写掩膜，
  标注却记账了。
- **反查成功率只有 49.4%**（363/735）：256 条是干净图已不在数据集里（类别是一一映射、
  干净图文件未动，所以迁移前后等价），111 条是文件名根本没有类别前缀
  （临时脚本 `_tmp_gen_diverse_15.py` 的非标准命名，`rejection.py` 从不支持），
  5 条源图来自已删除的 `演示素材_四类瓶子褶皱对比` 目录。

---

## 二、架构路线决策：转向 demo 架构（`../demo/`）

`../demo/` 是另一位同事做的 MVP，用户认为它的缺陷逼真度已达验收要求。本次做了三轮
对照实验来判断"是架构好还是素材好"，结论是**架构**。

### demo 架构是什么（三阶段，无掩膜）

- **阶段A 视觉分析**：`/chat/completions` + `gpt-5.6-sol`，输入目标图+参考图（原图，未 letterbox），
  输出严格 JSON（`depth_rating_1_to_5` / `coverage.percent` / `fold_count` /
  `geometry.angle_degrees` / `transfer_guidance` / `negative_constraints`），
  **按参考图 stem 缓存**
- **阶段B 约束生成**：letterbox 到 1024×1536 纯黑画布 → `/images/edits` 传
  `image[]`×2（目标+参考）+ 6KB prompt，**不传 mask**，`quality=low` 带降级链 →
  裁黑边还原到原始分辨率。prompt 把阶段A 的数值打上 `MUST MATCH EXACTLY`，
  但**位置只作软约束**（preferred zone，宁可放弃位置也不让褶皱跑到非标签区）
- **阶段C 光场校正**：ECC 亚像素配准（1/4 分辨率估仿射）+
  `out = gen - blur(gen,30) + blur(orig,30)`，即**中高频（褶皱+文字）全用模型的、
  低频（光照）全用原图的**。全图逐像素运算，**没有掩膜、没有区域概念**

实测（F09）：低频漂移 **9.19 → 0.75**，中高频保留 **0.9627**。
免疫重影的原因是**文字只有一个来源**（生成图的中高频），构造上不可能叠加。

### 三轮实验与结论

**第一轮：2×2 交叉盲评（24 项，4 格各 6）**

| | demo 素材 | 我们素材 |
|---|---|---|
| demo 架构 | 格D 4.67 / 验收 6/6 | 格A 3.33 / 验收 6/6 |
| 我们架构 | 格B 2.33 / 验收 6/6 | 格C 1.50 / **验收 3/6** |

**12 组配对比较全部指向 demo 架构，符号检验 p=0.00049**；架构效应 +2.08 分，
素材效应 +1.08 分且**两个方向都不显著**（p=0.076~0.087）。

**第二轮：跨产品泛化**（水次方深色瓶 / 透明蓝瓶 各 3 张）→ 验收 6/6，
与海昌无可检出差异（p=0.19~0.60）。反直觉的是**跨产品参考的蓝瓶拿了最高分**。

**第三轮：全产品覆盖**（17 项 = 5 产品 × 17 个类别各 1 张，参考从全库随机抽纯褶皱）
→ **验收 17/17**。

| 产品 | n | 验收 | 逼真度（第一轮尺度） |
|---|---|---|---|
| 120蓝瓶 | 1 | 1/1 | 4.50 |
| 500水次方 | 4 | 4/4 | 4.00 |
| 360森呼吸 | 4 | 4/4 | 3.75 |
| 500多效极润 | 4 | 4/4 | 3.75 |
| 60ml多效极润 | 4 | 4/4 | 3.25 |

**累计 demo 架构 35 个样本，验收 35/35；我们架构在自己最熟的类别上 3/6。**

### 实验方法上的几个可复用结论

- **评分噪声底线**：混入"用户之前评过的样本"当校准锚点，两轮独立测得的尺度偏移
  **都是 −0.50 分**，同图重评平均绝对差 0.50。**小于 0.5 分的组间差异不可信**。
  架构效应 2.08 = 4.2 倍噪声（可信）；跨产品差异 0.50 = 1.0 倍（检不出）。
- **参考 severity 与评分显著正相关**：rho=+0.550, p=0.023
  （sev3→2.00 / sev4→3.00 / sev5→3.67）。这是本次唯一显著的可操作发现。
- **同产品 vs 跨产品参考**：两轮方向相反且都不显著 → **没有证据支持同产品更好**。
  这对覆盖新瓶型是好消息（`60ml多效极润` 只能用跨产品参考）。
- **客观指标全线失效**：n=29（demo 架构），墨迹结构相关 / 低频光场漂移 / 变化像素占比 /
  死白 / 反推掩膜占比 **全部不显著**（p=0.29~0.90）。全样本上出现的
  "墨迹相关 rho=−0.676" 是**架构的代理**（该指标几乎完美区分两种架构），
  架构内部就没有关系了。**自动打分暂无可用代理指标。**
- **现有 VLM 质检与用户判断无关**：rho=+0.363, p=0.2466（n=12）。质检分全部落在
  5.75~8.35（都在合格线 5.5 之上），而用户只给 1~3 分。**质检语义需要重做**，
  且其中 `seam_continuity` / `blend_quality` 在无掩膜架构下已不存在这个概念。
  用户决定：**质检代码保留，暂不使用**，等找到真能对应的评分标准再启用。

实验的全部脚本与数据在 `../experiments/ab_arch/` 与 `../experiments/final_coverage/`
（在仓库外，不入库）。

---

## 三、demo 架构已定位的问题与优化点

### 已定位并给出数据的问题

**边缘伪影（根因已锁定，但视觉不可见）**

因果链：letterbox 贴图 → 模型输出 → 按标称边界 `(ox,oy,ox+nw,oy+nh)` 裁剪 →
**`ox` 那一列正好是"黑边→内容"的过渡列**（实测 F16：送进去 234，模型输出 92.3）→
这条暗列被裁进来 → 污染 30px 高斯低频，`blur(gen)` 在左缘被拉低 →
`delta = orig_low - gen_low` 被高估 → 阶段C 过度提亮 → 溢出 → `np.clip` 硬截断成 255。

**修法：裁剪内缩 1 像素**（`crop_and_restore` 一行），实测 n=17：

| 内缩 | 溢出像素 | 钉在255 | 低频残余漂移 |
|---|---|---|---|
| 0（demo 现状） | 5.048% | 0.1612% | 1.109 |
| **1** | 3.783% | **0.0210%** | **0.993** |

两项指标同时改善（纯赚）。F16 单独看：溢出 2.384%→0.000%，漂移 1.137→0.742。

**必须记下的更正**：这个问题**视觉上不可见**，用户看不出来是正常的——受影响像素在原图
里本来就是 233~234（近白的均匀背景），变成 255 只是亮了 9%，没有纹理损失。
我最初用的指标 `blown = (产出≥255) & (原图<245)` **阈值设得太宽**，把本来 234 的像素
也算成"新增死白"，严重夸大了问题。用修正阈值（原图<200，真正有害的烧白）重算，
所有配置下都只有 0.001%~0.004%。**曾提出的"限幅 + 软饱和"方案已撤回**——它治症状
且要付漂移代价（1.109→1.521），而内缩是治本。

**ECC 配准失败无检测**：`align_to_original` 只在 `cv2.error` 时回退，配准"跑通了却配歪"
没有防护。demo 自己的 `demo_from_5` 整体平移 44.25px、相关强度仅 0.121（本次 17 个样本
ECC 都正常，属低频高危）。

**`.bmp` 原样跑不通**：`image_data_uri()` 按扩展名发 `image/bmp`，接口只接受
jpeg/png/gif/webp → HTTP 400。转 PNG 即可（只改素材格式，不动算法）。

### 缺陷库的定位重划：**不需要重建**

demo 架构喂给模型的是**整张参考图**，它自己的阶段A 会重新分析。所以现有 838 条里
18 个字段只有 **4 个**还有用：

| 仍需要 | 用途 |
|---|---|
| `defect_type` | **筛掉划痕**。阶段A 的 prompt 预设"参考图里有褶皱"，喂它划痕图也会硬编出褶皱描述，不会报错——所以类型判断只能靠缺陷库 |
| `severity` | 已证明与评分正相关，免费的质量杠杆 |
| `class_name` | 产品/角度身份 |
| `source_image` | 定位文件 |

其余 14 个（`bbox` / `crop_path` / `extent` / `prompt_hint` / `geometry` /
`appearance` / `edge_profile` / `photometry` / `texture_effect` / `orientation` /
`count` / `location` / `source_size` / `entry_id`）都是为"裁块+掩膜+构造提示词"服务的，
现在是死重量。844 个裁剪图（511.7 MB）demo 架构不用，但**建议留着**——人工复核
"这条参考长什么样"时比翻整图快。

**参考池现状**（按源图聚合、要求整张图只含变形）：

| | 数量 | 按产品 |
|---|---|---|
| 纯变形源图 | 530 | 120蓝瓶 165 / 500多效极润 178 / 500水次方 187 |
| 其中 severity≥4 | 393 | 120蓝瓶 94 / 500多效极润 114 / 500水次方 185 |

---

## 四、已放弃的路线（代码保留，不再维护）

- **掩膜/标签自动生成**：用户明确放弃，YOLO 的框改为人工标注。
  `diff_defect_mask` / `mask_from_log_ratio` / `constrain_mask` / feather 与 poisson
  合成 / `ratio_composite` 等全套随之停用
- **flat-edit（平坦底图 + 乘性贴回）**：重影在构造上消失（墨迹相关 0.34→0.96），
  但盲评只有 1.50 分 / 验收 3/6，被 demo 架构完全压倒
- **relief-transfer（真实明暗场直接传递）**：几何保真是构造性的，但从未接入主流程；
  `../experiments/` 之外的对照图已加入 `.gitignore`
- **裁块相关**：`adaptive_patch` / `pick_patch_box` / `min_bottle_cover` 等（demo 编辑整图）
- **`displacement_field`**（用梯度伪造文字弯曲）：demo 整图重绘时文字**天然**就弯了
- **`align_photometry`**（全局增益+偏置）：被阶段C 的低频替换取代，后者能修**空间变化**的光场

**这次最重要的认知转变**：我们过去把"印刷内容逐像素保真"当硬指标去优化
（flat-edit 做到墨迹相关 0.96），但用户判它更不逼真。真正决定观感的是
**文字随褶皱物理弯曲**——这是整图重绘天然带来的，而掩膜内乘性回贴永远做不到。
方向从一开始就偏了。

---

## 五、待办

### P1（规模化的前置条件）

1. **薄适配层：把我们的工程能力包在 demo 的 `transfer_one` 外面，demo 算法零改动**。
   需要：并发（`max_workers`，demo 是串行 for 循环，**360×5产品=1800 张要跑 45 小时**）、
   断点续跑、失败分类落盘（`fail_log.jsonl`，demo 完全没有）、标注落盘、参考选择逻辑
   （**demo 没有"选参考"这个概念**，要求外部喂）。
2. **阶段A 结果做成全局参考分析库** `outputs/ref_analysis/`：
   `index.json`（抽 `depth_rating_1_to_5` / `coverage.percent` / `fold_count` /
   `direction` / `angle_degrees` 等可筛字段）+ 各参考的完整 JSON。
   收益：1800 张若复用几百个参考，分析调用从 1800 次降到几百次，一次投入永久复用；
   筛选维度比旧缺陷库更丰富（旧库没有 coverage / fold_count）。
   **注意**：阶段A 的 prompt 同时吃目标图和参考图，但 demo 的缓存**只按参考图命名**，
   即已经在跨目标复用（35/35 通过的产出就是这么跑的）。预建库符合既有行为，
   只是 `transfer_guidance` 里针对目标的措辞会略有错配，需记在案。
3. **360森呼吸 编目**：327 张缺陷图完全没编目 → 拿不到 `defect_type` → **进不了参考池**
   （它的 456 张干净图仍可当目标，本次 4 个角度都跑过了）。
   建议写**精简编目**（只判"褶皱/划痕/其他" + 1-5 强度），不需要 bbox 和裁剪图，成本远低于现有 `build-catalog`。

### P2（已有证据的改进）

4. **`min_reference_severity` 从 3 提到 4**（本次 rho=+0.550, p=0.023 支持）。
   参考池 sev≥4 有 393/530 张，量足够。
5. **裁剪内缩 1 像素**（`crop_and_restore` 一行，两项指标同时改善，但**视觉不可见**，
   顺手改即可，不值得单独排一轮）。
6. **ECC 配准加成功性校验**：配准后校验相关性有没有改善，没改善就退回恒等变换；
   再加仿射分量边界（缩放偏离 >2%、平移 >20px 直接拒绝）。
7. **`.bmp` 转 PNG**（一行，或在适配层统一做素材预处理）。
8. **试更大画布**（未验证，潜在收益最大）：1024×1536 让 1200×2400 缩到 0.64x 丢细节、
   让 675×1370 放大 1.12x 放噪点。gpt-image-2 按 config 注释支持 2K/4K，
   需实测更大档位的 504 超时率。
9. **区域约束从软改硬**（针对 360° 场景）：某些角度标签膜面积很小甚至不可见，
   软约束下模型可能把褶皱画到瓶盖/桌布。可先用分割算标签膜占比，过低的角度直接跳过。

### P3（自动打分，两条路）

10. **路径A：把目标从"预测 1-5 分"改成"检出明显失败"**。理由：累计 35/35 全通过，
    失败率低到验收标准从没被触发——真正需要的是在 1800 张里挑出那几十张异常。
    而这些**恰恰是客观指标能测的**：配准失败（相位相关平移+强度，已抓到 44px 实例）、
    边缘伪影、死白连通域、黑块（局部亮度直方图异常）、褶皱画到桌布（分割求交）、
    整体没生成（变化占比过低）。**不需要新数据，规模化必需，建议先做。**
11. **路径B：VLM-as-judge，用已有的 35 个人工分当验证集**。
    - **第一步必须先算天花板**：用两轮共 10 对重测数据算用户的自我一致性。
      如果自我一致性只有 r=0.8，任何评分器都不可能超过 0.8。零成本，决定这条路的目标值。
    - 第二步**倒过来做**：先让 VLM 对 35 张自由描述"哪里像真的、哪里像假的"，
      看它的描述与高/低分的相关，**用数据找判据而不是猜一套维度**。
    - 第三步迭代 prompt，**必须 5 折交叉验证**（n=35 太小，单次留出不可靠，
      prompt 迭代只能看训练折）。判定：rho≥0.7 可当闸门，0.5~0.7 只能粗排序，<0.5 走路径A。
    - 同时评二分类版本（能否分开 1-2 分与 4-5 分，算 AUC）——比预测精确分数简单且实用价值更高。
    - 成本每轮 35 次调用；风险是 n=35、两端各约 5 个样本，prompt 容易过拟合。

### P3（360° 场景的设计要点）

12. **train/val 必须按角度分块切分，不能随机切**。360 个角度里相邻角度几乎一模一样，
    随机切分会让验证集里每张图在训练集里都有 1° 之差的近孪生 → **验证 mAP 严重虚高**，
    得到一个看起来很好但泛化很差的模型。这一条不注意，后面所有训练结论都不可信。
13. 每张图配一个 sev≥4 的纯褶皱参考；同一角度出多版时用不同参考即可获得形态多样性。
    物理一致性不用管（YOLO 是单帧检测器，不看角度间的时序一致性）。

### 已知问题（暂不处理，但要知道）

14. **驳回样本的 mask 从未写出**：56 条标注记录了指向 `outputs/rejected/` 的 mask 路径，
    但那里从来没有 `*_mask.png`。流水线只写图不写掩膜，标注却记账了。
15. **反查成功率 49.4%**：256 条干净图已不在数据集、111 条文件名无类别前缀
    （临时脚本的非标准命名）、5 条源图来自已删除的演示目录。
16. **5 条标注的 `class` 停留在产品级**（`360森呼吸`，无角度后缀）：干净图来自已删除的
    `演示素材_四类瓶子褶皱对比` 目录，stem 在现存数据集里查不到，**角度不可知，
    没有猜一个填进去**。
17. **最根本的开放问题：合成数据对 YOLO 训练是否真有帮助，始终未验证。**
    只能靠实际训练对比回答，本次全部实验都不触及这一点。

---

## 六、新会话上手提示

- 迁移后的类别名是 `<产品>_角度<N>`，命令行示例见 `README.md`（分工表已按角度重排）。
  `dataset.product_of(class_name)` 取产品名，`same_product(a, b)` 判是否同产品。
- 想验证数据一致性：`python tools/verify_product_migration.py`（退出码非 0 表示有
  **迁移引入**的破损；"迁移前既有问题"已作为基线排除并单独打印）。
- 想撤销目录改名：`python tools/migrate_classes_by_product.py --undo product_migration_reverse_map.json`
  （缺陷库/标注用 `.pre_product_migration` 备份恢复）。
- demo 架构的调用封装在 `../experiments/ab_arch/run_demo_arch.py`：**import demo 的模块、
  只替换两个 API URL 常量，不改他们一行代码**。任何效果差异都不可能是改坏的。
  兼容性已核对：chat 模型双方都是 `gpt-5.6-sol`、编辑模型都是 `gpt-image-2`、
  multipart 字段名都是 `image[]`，唯一差异是端点路径。
